import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mkundalik.app',
  appName: 'mkundalik',
  webDir: 'build',
  // server: {
  //   url: "https://mkundalik.uz",
  //   cleartext: true
  // }
};

export default config;